# Copy to .env and adjust. Do not commit .env with real credentials.
# PostgreSQL (localhost) - password in URL must be URL-encoded: @ -> %40, ? -> %3F
DATABASE_URL=postgres://postgres:p%40s3w0rd%3F1995@localhost:5432/fdms

# Optional: sync data to another Postgres (manage.py sync_to_remote_db)
# SYNC_TARGET_DATABASE_URL=postgres://admin:netmannature@localhost:5432/fdms_db

# Optional for local dev
# DJANGO_SECRET_KEY=your-secret-key
# DJANGO_DEBUG=true
# CELERY_BROKER_URL=redis://127.0.0.1:6379/0
